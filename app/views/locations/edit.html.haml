- cache do
  - title "Edit #{@location.code}"
  - content_for :breadcrumb do
    = breadcrumb [ link_to("Administrations", administrations_path), |
      link_to("Warehouses", warehouses_path), |
      link_to(@location.warehouse.code, warehouse_path(@location.warehouse)) ] |

  = sidemenu

  = render "form"

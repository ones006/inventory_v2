<% cache do %>
  <% title 'Create new location' %>
  <% content_for :breadcrumb do %>
    <%= breadcrumb [ link_to("Administrations", administrations_path),
                     link_to("Warehouses",warehouses_path),
                     link_to(@location.warehouse.name, @location.warehouse),
                     "Create new location under #{link_to @location.warehouse.name, warehouse_path(@location.warehouse)}" ] %>
  <% end -%>

  <%= sidemenu @tab %>

  <%= render :partial => "form" %>
<% end -%>

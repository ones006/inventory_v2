- cache do
  - title "Edit Transaction Type #{@transaction_type}"

  - content_for :breadcrumb do
    = breadcrumb [ link_to('Transactions', transactions_path), |
      link_to("Transaction Types", transaction_types_path), |
      "Edit transaction type #{link_to(@transaction_type.code, @transaction_type)}" ] |

  = sidemenu @tab

  = render :partial => 'form'
